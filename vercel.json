{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "pnpm db:generate && pnpm exec prisma migrate deploy --schema=db/schema.prisma && pnpm build",
  "installCommand": "pnpm install --frozen-lockfile"
}
